{
  "name": "@ari/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for ARI",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
